import torch
import torchvision
from torch import nn, optim

def train(num_workers=4):
    # dataGenerator = icdar.get_batch(num_workers=num_workers,
    #                                 training_data_path='./Dataset/Train',
    #                                 batch_size=24)

if __name__ == '__main__':
    train()
